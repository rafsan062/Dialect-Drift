{
  "name": "dialect-drift",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run bundle-vendor && vite build",
    "preview": "vite preview",
    "live": "npm run build && vite preview --port 5500",
    "bundle-vendor": "node scripts/bundle-vendor.mjs",
    "postinstall": "npm run bundle-vendor"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "d3-cloud": "^1.2.7",
    "topojson-client": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vite": "^5.4.11"
  }
}
