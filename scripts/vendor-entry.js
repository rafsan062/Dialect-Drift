/** Bundled for Live Server — no CDN required for D3 / word cloud. */
import * as d3 from "d3";
import { feature } from "topojson-client";
import cloud from "d3-cloud";

export { d3, feature, cloud };
